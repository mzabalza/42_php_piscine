#!/usr/bin/php
<?PHP
	
	if ($argc < 2)
		exit (1);
	function ft_split($str)
	{
		$exploded_str = explode(" ", $argv[1]);
		$rtn = array_filter($exploded_str);
		return ($rtn);
	}
	$i = 0;
	while(for
	{

	}
	$splited_array = ft_split($argv[1]);
	sort($splited_array);
	foreach($array as $elem)
		echo "$elem";
?>